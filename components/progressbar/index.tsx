import {
  ProgressBar as ProgressBarPaper,
  ProgressBarProps,
} from "react-native-paper";

const ProgressBar = (props: ProgressBarProps) => {
  return <ProgressBarPaper {...props} />;
};

export default ProgressBar;
